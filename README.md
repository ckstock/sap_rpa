# SAP 自动化执行门户

静态前端门户，用于从网页发起 SAP 事务码自动化执行。

当前重点：

- `ZFI019NL` 周损益执行链路展示。
- 通过 `sap-rpa://` 唤醒本机 `SapWebLauncher`。
- 兼容旧版 `sap-zck://`。
- Bridge API 预留状态回传能力。

本机启动器源码在本地目录：

```text
D:\工作\网页启动登录
```
