# 反思：SAP RPA V2 服务器上线坑点

## 结论

这次安装包本身可以把页面、执行器、VBS、SQLite 和本地 API 部署起来，但安装说明不够完整：路径、真实 JSON 配置、SAP GUI 组件、源码目录和运行目录的边界没有在上线前强制确认，导致多次靠运行报错才发现缺项。

后续上线必须先拿到并确认这些参数，再部署：

| 必填项 | 示例 | 风险 |
| --- | --- | --- |
| 源码仓库目录 | `D:\RPA\RpaProject` | 只 `git pull` 不会影响线上运行。 |
| 运行根目录 | `D:\RPA` | 线上实际读取 `bin/assets/transactions/data/logs/config.local.json` 的地方。 |
| 后端 exe | `D:\RPA\bin\SapWebLauncher.exe` | 改了 `Program.cs` 必须 publish 并复制到这里。 |
| SAP 登录配置 | `%LOCALAPPDATA%\SapWebLauncher\config.json` | 必须在服务器固定 Windows 执行账号下生成。 |
| 钉钉真实配置 | `D:\RPA\config.local.json` | 只有模板不会推送钉钉。 |
| 对外入口 | `https://fi_automation.srv.lstech.com/rpa/` | 正式用户链接，需要单独确认 DNS、443、证书、路径前缀和防火墙。 |
| 兼容入口 | `http://<服务器IP>:6174/rpa/` | 保留给内网端口访问和排障；6174 是 HTTP，不是 HTTPS。当前联调服务器是 `10.0.41.158`，正式系统 IP 是 `10.0.2.120`。 |
| HTTPS 证书目录 | `D:\RPA\certs\lstech.com` | 证书私钥、PFX 和密码文件只能留在服务器本机，不能提交 GitHub。 |
| SAP GUI COM 注册 | `D:\RPA\启动脚本\00_register_sap_gui_components.cmd` | 生产机首次部署或 SAP GUI 重装后必须检查，避免手工 SAP 已登录但程序仍反复登录。 |
| 功能说明书同步 | `SapRpa_V2_功能说明书.html` / `fs-skill` | 跨层变更后不更新文档，下一次 agent 或人工上线会继续踩同一个坑。 |

## 已踩坑点

1. `config.local.example.json` 只是模板，不会发送钉钉。正式推送必须在运行根目录创建真实 `config.local.json`，并填写 `dingTalkOpenApi.baseUrl/appKey/appSecret/agentId`。
2. “勾选固定通知 11464769”只说明收件人 ID 传入了，不代表后端具备 DingTalk OpenAPI 凭证。缺凭证时日志会出现 `sap dingtalk openapi skipped: missing baseUrl/appKey/appSecret/agentId config`。
3. `baseUrl` 只能填接口根地址，程序会自动拼接 `/token` 和 `/dingtalk-oa/topapi/message/corpconversation/asyncsend_v2?token=...`。
4. 源码目录和运行目录不是一回事。`git pull` 后必须 `dotnet publish`、复制到运行目录 `bin/assets/transactions`，并只重启本项目 `SapWebLauncher.exe --serve`。
5. 前端已拆为 `index.html + assets\js`，只复制 `index.html` 会造成页面白屏或按钮无响应。
6. VBS 在 Git 里维护，但真实执行读取运行目录 `transactions`。只改源码 VBS 不复制到运行目录，网页仍跑旧脚本。
7. SAP GUI 手工登录正常，不等于脚本组件可用。出现已登录但程序仍反复登录、检测不到 ready session 时，要检查 SAP GUI Scripting、`saprotwr.dll` 和 `sapfewse.ocx` 注册状态。
8. SAP GUI 自动化必须运行在已登录的交互式桌面会话里，不应做成普通 Windows Service 跑在 Session 0。
9. 部署和重启只能动本项目进程和端口，不要误杀其他项目的 `node.exe`，不要占用别人已有的 80/6173。
10. 上线验收不能只看 API health。还要检查页面、`assets\js`、VBS、SQLite、SAP 登录配置、钉钉真实配置和真实任务日志。
11. 域名 HTTPS 不是把 6174 改成 HTTPS；正确做法是保留 6174 HTTP 兼容入口，再用 443 HTTPS 网关反代 `/rpa/` 和 `/rpa/api/*`。
12. 浏览器 localStorage 可能残留旧 API 地址，网关必须按当前 `window.location.origin + "/rpa"` 注入并覆盖，前端要优先读网关注入值。
13. 证书目录权限要收紧到固定执行账号、Administrators 和 SYSTEM；普通 Users 不应读取私钥、PFX 或 `passwd.txt`。
14. 服务器重启后不能只启动 API；还必须启动 `D:\RPA\gateway\rpa-gateway.js` 的 6174 HTTP 网关和 443 HTTPS 网关。总入口统一放在 `D:\RPA\启动脚本\start_sap_rpa_services.cmd`。
15. `D:\RPA\启动脚本` 是线上运行入口，`D:\RPA\RpaProject\启动脚本` 是源码副本；两处脚本默认都操作 `D:\RPA`，不要把源码副本当作独立测试环境。
16. 按 `fs-skill` 规则，前端、后端、VBS、SQLite、钉钉、SAP 登录、网关、启动脚本、安装包或上线步骤发生变化时，必须同步功能说明书、安装文档清单和本反思文档，不能只靠聊天记录或口头说明。
17. GitNexus 已安装为 npm 全局 CLI，路径是 `C:\Users\Marcus\AppData\Roaming\npm\gitnexus.cmd`。旧终端如果还识别不到 `gitnexus`，是进程 PATH 未刷新；优先用 `D:\RPA\启动脚本\gitnexus.cmd status` 或 `D:\RPA\启动脚本\gitnexus.cmd detect-changes`，脚本会自动切到 `D:\RPA\RpaProject`，必要时再用仓库根目录的 `node .gitnexus\run.cjs status` 兜底。
18. `D:\RPA` 可以整包拷贝到生产机，但只能把它当作程序包。测试机的 `config.local.json`、SQLite、证书、日志、输出和 `%LOCALAPPDATA%\SapWebLauncher\config.json` 不能默认覆盖生产机状态。
19. SAP GUI 组件注册应该在生产机安装脚本里前置。`D:\RPA\启动脚本\00_register_sap_gui_components.cmd` 负责管理员提权注册 `saprotwr.dll` 和 `sapfewse.ocx`；`00_register_sap_gui_components.ps1 -CheckOnly` 和 `check_sap_rpa_services.cmd` 负责只读检查两个 COM ProgID 是否为 `True`。
20. 内网服务器可能访问不到 HTTPS 证书吊销服务器，导致 PowerShell `Invoke-WebRequest` 报 `CRYPT_E_REVOCATION_OFFLINE` 或 TLS 通道错误。健康检查脚本应使用 `curl.exe --ssl-no-revoke` 做 GET 检查；这只跳过 CRL/OCSP 吊销查询，不跳过证书链和域名校验。
21. SAP GUI 组件注册不能固定写死一个 `regsvr32.exe`。32 位组件要用 `C:\Windows\SysWOW64\regsvr32.exe`，64 位组件通常用 `C:\Windows\System32\regsvr32.exe`；如果从 32 位 PowerShell 进程注册 64 位组件，要用 `C:\Windows\Sysnative\regsvr32.exe` 避免文件系统重定向。注册脚本必须读取 DLL/OCX 的 PE machine 类型并在 `-CheckOnly` 输出里打印实际选择。
22. 定时任务列表里“状态”和“设置人”不能混用。`status/enabled` 仍表示启用/停用，设置人必须用独立列展示 `updated_by || created_by`；否则用户无法判断任务是否启用。
23. 定时任务通知不能只存 `notifyTarget=dingtalk`。保存时要把当前用户钉钉 ID 存为 `dingtalk:<钉钉ID>`，调度器创建 run 时解析到 `runs.ding_talk_user_id`，执行完才能推送给设置该定时任务的人；开始、成功、失败三个开关也要分别落到数据库并在发送阶段再次判断，不能只在页面上显示。
24. `/NEX` 清理不能只覆盖某个事务码。子任务可以延迟到父批次结束统一清理，但普通单任务完成后也必须执行 SAP GUI 清理，避免服务器桌面堆积 SAP 窗口。
25. 定时任务需要真正删除入口。用户设置错后，页面删除按钮应调用 `DELETE /api/schedules/{id}`，从 `schedule_tasks` 删除配置；已产生的 run 历史保留用于追溯。
26. SAP 多重登录策略必须写成明确配置，不能只靠口头约定。当前默认 `multiLogonPolicy=takeover`：服务器登录同一 SAP 账号时，如果 SAP 弹出多重登录确认，程序会选择继续本次登录并终止该账号其他终端登录；如生产不允许踢人，必须在 `D:\RPA\config.local.json` 或环境变量里改成 `fail`。
27. “运行前干净登录”分两层：服务器本机残留的 SAP 登录/多重登录窗口可以在登录前清理；其他电脑上的同账号会话只能在服务器发起登录、SAP 弹出多重登录确认后由 takeover 终止。不能把这件事误写成无条件远程 kill 所有电脑进程。
28. `/NEX` 只适合已经登录且命令框可用的 SAP session。登录前窗口、多重登录弹窗、密码错误框、SAP Logon 主窗口不一定能通过 `/NEX` 清理，清理脚本必须遍历多个 session，并对没有 okcd 的窗口尝试关闭和记录日志。
29. VBS 修复不能只停在源码目录。`D:\RPA\RpaProject\网页启动登录\transactions\ZFI057.vbs` 改完后，真实运行目录 `D:\RPA\transactions\ZFI057.vbs` 也必须同步；否则网页仍然加载旧脚本，旧剪贴板内容会继续被粘进 SAP。
30. ZFI057 的物料集不能直接塞进 VBS 超长字符串字面量。后端应把物料集写入临时 UTF-16 文件，并传 `@file:<path>` 给 VBS 读取；这样可避免几百/上千物料导致 WSH 解析、杀软拦截或 cscript 启动异常。
31. SAP 多选粘贴前必须有剪贴板读回校验。仅输出 `material clipboard prepared count=536` 只能说明脚本尝试写剪贴板，不能证明 SAP 粘贴时剪贴板已被覆盖；验收日志必须出现 `clipboard verified ... lines=<materialCount>`，且随后才允许 `pressed paste S_MATNR material list`。
32. ZFI057 的多工厂口径只适用于 ZFI057 自动三步工作流，不能扩散到 ZFI072A、ZFI080、ZCO019 等其他事务码。后端应按业务范围只读查询 `ZFIT_RPA_BUKRS-GSBER`，取全部 `WERKS` 后逐工厂发起步骤二；每次 VBS 只收到一个工厂并写入 `S_WERKS-LOW`，不得把多个工厂一次性写入多选。
33. 统计报表默认周期必须符合用户可见语义。用户说“今天运行的事务码”时，页面默认应统计当天；如果页面允许调整统计周期，事务码排行、执行总次数、历史列表和导出 CSV 必须使用同一套 `from/to`，不能统计页按日期过滤而导出/历史仍取最近若干条。
34. SAP 多重登录弹窗处理不能只选中 `MULTI_LOGON_OPT1` 就返回成功。必须继续按确认按钮或发送 Enter，并等待弹窗关闭或 `session.Info.User` 变成目标用户；同时外层 `cscript //T` 超时必须长于内部等待时间。否则日志会看起来“已选择接管”，但实际仍停在 `SAPMSYST screen=500`，后续不会进入 `/n事务码`，也不会加载 VBS。
35. HTTP 兼容入口里的 IP 不能写死联调服务器 `10.0.41.158`。它必须取目标服务器本机 IPv4，可在服务器上运行 `ipconfig` 或 `Get-NetIPAddress -AddressFamily IPv4` 查看；正式系统 IP 是 `10.0.2.120`，兼容入口应是 `http://10.0.2.120:6174/rpa/`。启动/检查脚本支持 `-HttpCompatibilityHost 10.0.2.120` 或环境变量 `RPA_HTTP_COMPATIBILITY_HOST=10.0.2.120`。
36. ALV 的组织归档不能只使用请求参数里的第一个工厂。一个导出 Excel 可能包含多个 `WERKS`，同一工厂也可能在 `ZTFI48B` 映射到多个 `ZBU/ZSBU`。后端必须按 Excel 实际 `WERKS` 或 `GSBER` 分组，逐组只读查询 `ZTFI48B` 或 `ZTFI48A`，按 `<ZBU>\<ZSBU>\yyyy_WKnn` 归档；同组织的数据合并，一厂多组织各写一份。只有查询成功但没有映射才允许回退到 `<集采工厂>\yyyy_WKnn`；业务范围表仍有 `WERKS` 时，回退文件也必须使用该工厂号，不能只按 `GSBER` 覆盖不同工厂。查询失败、缺字段或缺来源值必须判为失败并保留暂存，不能拿本地 SQLite、前端入参或具体工厂硬编码补值。

37. 网络盘导出必须分两段验证：SAP GUI 仅写本机 `ALV本地暂存`，后端以实际执行账户复制到 UNC 并校验文件长度后才算归档成功。交互式终端账户访问 UNC 被拒绝，不能据此判定生产服务无权限；必须检查运行账户的真实复制结果。执行器重启后，旧进程留下的 `running` 子任务必须立即失败并通知，且保留本机暂存供排障，不能让 6 小时超时锁住后续导出。

38. 重启恢复和 6 小时守护只能回收带桥服务队列租约的任务，不能把 `sap-rpa://` fallback 进程标记失败或与之并发操作 SAP GUI。暂存目录必须固定在 `D:\RPA\临时文件\ALV本地暂存` 或其子目录；禁止配置成 UNC，也禁止与最终归档根目录相同。

39. 生产 `fileStorage.alvExportDataDirectory` 是 UNC 时，API 与监控必须由已获该共享权限的固定交互式 Windows 账户启动。不能用权限不同的部署/诊断账户手工重启后就判断网络盘失效；该账户会在初始化创建归档根目录时失败或阻塞。发布时可临时停止 `SAP_RPA_StartupMonitor` 防止旧进程回拉，发布后必须重新启用它，并用该生产账户运行的项目启动/检查脚本验收。

40. 运行目录的写入权限和 API 进程控制权限必须与固定交互式执行账户一并验证。即使部署会话属于 Administrators，也可能无法停止 `marcus` 拥有的 `SapWebLauncher.exe` 或覆盖已加载的 `D:\RPA\bin\SapWebLauncher.dll`。升级前先确认队列为空，再在该账户会话或具备其进程控制权限的受控运维会话中依次暂停 `SAP_RPA_StartupMonitor`、停止 API、复制 DLL/PDB 与同批 VBS/前端文件、恢复监控并运行项目检查脚本。复制 DLL 失败时必须还原已经同步的 VBS/前端，不能留下新脚本配旧后端的混合版本。

41. 导出 Excel 是业务结果，不得由 Codex、ChatGPT 或诊断工具自动打开、预览或读取内容。只允许 SAP 自动化按本次完整路径关闭其自身导出的工作簿；用户手工查看时使用 Windows 默认 `.xlsx` 关联的 Excel。验证归档时只检查文件存在、长度、哈希和运行日志，不打开报表。

42. 配置页的“删除”必须是真实删除而不是停用。后端删除前检查定时任务、排队/运行任务和机器人绑定等引用；删除默认播种配置后还要保留删除标记，防止服务重启时配置复活。`ZCO019` 定时任务必须把“明细保存”和“汇总保存”分别保存为 `runStrategy=detail|summary`，历史无模式任务兼容为明细加汇总。

## 后续安装包必须做到

1. README 开头必须列出路径和 JSON 配置清单，让管理员上线前逐项确认。
2. 不再提供一键安装器作为正式入口；安装包只保留手工安装指南、上线核对清单和必要脚本。
3. 上线核对清单必须检查 `config.local.example.json` 和真实 `config.local.json`，并验证 `baseUrl/appKey/appSecret/agentId` 四项完整。
4. 文档必须明确真实 `config.local.json`、SAP 密码、SQLite、日志和输出文件不能提交 GitHub。
5. Git 更新说明必须写清楚：后端改动要 publish 到运行目录 `bin`；前端改动要复制 `index.html + assets`；VBS 改动要复制到运行目录 `transactions`。
6. 验收不能停在 health check，至少要从网页入口跑一次受控任务，确认数据库有 run/log，通知启用时确认日志出现 `sap dingtalk openapi sent`。
7. 每次改部署、脚本、配置或业务链路后，都要按 `fs-skill` 同步功能说明书和上线安装文档，让后续 agent 不依赖历史聊天上下文。
8. 生产机脚本必须把 SAP GUI COM 注册状态作为前置或验收项，不能只等真实任务反复登录后再手工排查。
9. 如果采用整包拷贝 `D:\RPA`，文档必须明确“程序文件可覆盖、生产状态默认保留”的边界，并要求拷贝前备份生产机真实配置、SQLite 和证书目录。
10. HTTPS 验收脚本必须区分“服务不可达”和“内网 CRL/OCSP 吊销查询不可达”。脚本可以跳过吊销查询，但不能跳过证书链和域名校验，最终仍要用浏览器确认无证书告警。
11. SAP GUI 组件注册脚本必须兼容 32/64 位 SAP GUI，不能只按当前测试机的 `Program Files (x86)` 场景写死 `SysWOW64`。
12. 定时任务功能验收必须覆盖：状态启停仍可用、设置人独立展示、保存后 `notify_target` 含钉钉 ID、`notify_on_start/success/failure` 三个开关按页面勾选落库、执行完成推送给设置人、删除后 `schedule_tasks` 中配置行不存在。
13. ZFI057 的业务范围到工厂映射必须单独验收：运行 `SapWebLauncher.exe --test-zfi057-bukrs-mapping --businessArea 2800`，确认只读查询 SAP 表 `ZFIT_RPA_BUKRS` 后返回该 `GSBER` 的全部 `WERKS`。这是表字段语义，不依赖 SAP 集或网关 action。
14. ZFI057 不能保留任何“看起来只是兜底”的旧工厂映射。步骤一只按业务范围和日期取物料；步骤二的工厂必须来自 `ZFIT_RPA_BUKRS` 中 `GSBER=业务范围` 的全部 `WERKS`，同一 `GSBER` 多行时必须全部取出且逐个执行，不能只取第一条或最后一条。VBS 不能再根据业务范围硬编码单工厂，通知展示也不能为了补工厂而二次查 SAP 或读本地旧表。
15. 用户可见通知不能直接暴露 SAP 技术字段名。`ZFI057` 跨月拆窗的钉钉提示应显示“成本核算日期”和“成本核算日期起于”，内部日志仍可保留 `S_KADKY/S_KADAT` 便于排错。
16. `ZFI057` 的日期拆窗必须同时改后端摘要和真实 VBS 入参。`S_KADKY-LOW` 取窗口月份 1 号，`S_KADAT-LOW`（成本核算日期起于）取窗口月份 2 号；跨月时要按发布成本月（1、3、5、7、9、11 月）从截止月份往前回推，可能生成 1 到 3 个窗口，而不是固定两段。只改钉钉展示不够，必须同步 `网页启动登录\transactions\ZFI057.vbs` 并发布到 `D:\RPA\transactions` / `D:\RPA\bin\transactions`。
17. `ZFI057` 的“没有符合条件数据”不能只按整支 VBS 失败判断。跨月时一次步骤二会跑多个日期窗口，可能第一窗口有数据、第二窗口无数据；这时步骤二已经有数据，必须继续步骤三。VBS 应按窗口记录 no-data，只有本次步骤二所有窗口都无数据时才返回 no-data 给后端跳过。
18. SAP 登录链路验收必须看真实事务日志。多重登录接管修复后，受控任务日志至少要出现 `SAP GUI login is ready`、`加载外部事务码脚本`、`执行 VBS`、`INFO: transaction=<事务码>`、关键字段写入日志和 `CLEANUP: sent /nex`；只看到 `selected MULTI_LOGON_OPT1` 不能算通过。
19. ALV 导出验收不能把 SAP 状态栏“已传递 xx 个字节”误判为异常；这通常表示前端导出已完成。含导出动作的事务必须以导出文件存在且大小稳定作为继续下一工厂的门槛，并处理 SAP GUI 标准导出短暂拉起 Excel 的副作用。清理时只能按完整路径关闭本次导出文件，父任务兜底也必须从 child run 的 `run_files` 读取分片路径后调度安全关闭 helper；不能按窗口标题去 kill Excel 进程，避免误关用户手工打开的文件。延迟关闭 helper 不能留在业务数据目录，VBS 放 `%TEMP%\sap_rpa_excel_close` 并自清理，后端兜底放 `D:\RPA\logs\excel-close`。
20. ALV 导出规则变更后，旧的“父任务合并总 Excel”验收口径必须同步删除。采购价链路现在按周、工厂两层生成文件，例如 `2026_WK31\6700\ZFI072N_维护采购价_工厂6700_yyyyMMddHHmmss.xlsx`；输出根目录来自 `D:\RPA\config.local.json` 的 `fileStorage.alvExportDataDirectory` 或环境变量 `SAP_RPA_ALV_EXPORT_DIR`。父 run 只收集 child 的最终工厂文件，不再生成“合并数据”或“合并异常”工作簿。ZFI072N 跨月等同一工厂多窗口场景会先导出 `_part1/_part2`，再合并成一个工厂 Excel 并删除 part。验收要看周目录下每个工厂目录、最终 `OUTPUT_FILE`、父 run 文件列表和 `ALV plant parts merged` 日志，而不是找跨工厂总表。
21. 从 2026-08-06 起，上一条“周/工厂目录”验收口径已被组织归档替代：验收优先查 `<alvExportDataDirectory>\ZBU\ZSBU\yyyy_WKnn\事务码_卡片名称_WKnn.xlsx`；无映射时查 `<alvExportDataDirectory>\集采工厂\yyyy_WKnn`。需要验证三个异常面：同一 Excel 多工厂分别路由、一厂多组织复制、重跑只替换同来源行。真实 SAP 导出成功不等于整个卡片成功，网络归档映射或写入失败时必须失败并进入钉钉通知。

22. 前端不能把“门户工厂主数据中不存在/停用”与“用户在业务范围或事务码规则中显式配置的 SAP 工厂代码无效”等同起来。配置刷新若对 `plantGroups` 或事务规则做全量 known-plant 过滤，会导致用户新增工厂在保存后消失，表现为输入框清空且联动失效。正确处理是保留显式代码、在规则弹窗即时展示工厂标签与关联业务范围；没有主数据或业务范围时给出缺失提示，由用户补齐主数据，而不是静默删值。新增输入框只能在新增成功后清空，重复输入必须明确提示。
23. ZFI057 的业务范围到工厂查询只使用 SAP 表 `ZFIT_RPA_BUKRS`：`GSBER` 匹配业务范围，`WERKS` 是可执行工厂。当 `ZFI019NL` 已取到物料、但该表没有维护可执行工厂时，这不是无数据，也不是 SAP GUI 或 Excel 故障；必须标记范围失败，并给出“维护 ZFIT_RPA_BUKRS-GSBER/WERKS 映射或从任务范围移除”的中文可操作提示。
24. 事务码自动化直接打开哪个 SAP 事务，和该事务在 SAP 内部绑定到哪个维护程序是两件事。代码审计只能证明 ZFI057 工作流执行 `/nZFI057` 及后续 `ZCO020`，不能据此臆断或宣称调用 `ZFI085_MAINTAIN`；内部程序映射必须在 SAP `SE93` 做只读核验后再写入功能说明。

## 当前服务器状态记录

- 当前运行根目录：`D:\RPA`
- 当前源码目录：`D:\RPA\RpaProject`
- 当前正式用户入口：`https://fi_automation.srv.lstech.com/rpa/`
- 当前兼容入口：按服务器 IP 生成。当前联调服务器为 `http://10.0.41.158:6174/rpa/`，正式系统为 `http://10.0.2.120:6174/rpa/`。
- 当前本机 API：`http://127.0.0.1:8080`
- 当前真实钉钉配置：`D:\RPA\config.local.json`，只保存在服务器本机，不提交 Git。
- 当前 GitNexus：CLI 版本 `1.6.9`，`gitnexus status` 显示当前索引 up-to-date；`doctor` 显示 graph/FTS 可用、VECTOR 不可用但 exact-scan 可用。
